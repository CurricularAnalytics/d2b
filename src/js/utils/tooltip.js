d2b.util.tooltip = function () {
  const $$ = {};

  const tooltip = function (g) {

		return tooltip;
  };

  /* Inherit from base model */
  const model = d2b.model.base(tooltip, $$);

  return tooltip;
};
